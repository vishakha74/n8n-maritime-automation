# Diagrams

| File | Shows |
|---|---|
| `system-overview.svg` / `.png` | The three workflows, their triggers, outputs and Ollama |
| `vessel-route-engine.svg` / `.png` | Every stage of workflow 02, coloured by LLM / deterministic / external |
| `linkedin-cover.svg` / `.png` | 1200×627 image for sharing the repo |

SVGs are the source. PNGs were rendered from them at 2× (cover at 1×).
